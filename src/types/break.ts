export interface Break {
  breakID?: string;
  note?: string;
  date: string;
  durationMinutes: number;
}

export interface BreakTableRows {
  breakID?: string;
  note?: string;
  delete?: string;
  date: string;
  durationMinutes: number;
}
